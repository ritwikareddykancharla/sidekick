{
  "name": "sidekick-root",
  "scripts": {
    "build": "cd frontend && bun install && bun run build",
    "start": "cd frontend && bun install && bun run build && cd .. && python3 -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
  }
}